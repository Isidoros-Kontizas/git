const arr = [1, 2, 4, 6, 10, 20];

const arr2 = arr.slice(0, 4); //first number is the begining, second number is the end but the value is NOT included

console.log("This is arr:", arr);
console.log("This is arr:", arr2);


const arrSplice = [1, 2, 4, 6, 10, 20];

const arrSplice2 = arr.slice(0, 4);